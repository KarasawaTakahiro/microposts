<%= form_for(current_user.favorite_relationships.find_by(micropost_id: micropost.id),
             html: {method: :delete},
             remote: true) do |f| %>
    <%= hidden_field_tag :micropost_id, micropost.id %>
    <%= button_tag class: "btn btn-link" do %>
        <%= content_tag :span, "", class: "glyphicon glyphicon-heart btn-favorite" %>
    <% end %>
<% end %>